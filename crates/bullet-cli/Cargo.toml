[package]
name = "bullet-cli"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true

[dependencies]
bullet-backtest = { path = "../bullet-backtest" }
bullet-core = { path = "../bullet-core" }
bullet-data = { path = "../bullet-data" }
libc = "0.2.189"

[dev-dependencies]
arrow-array = "59.1.0"
arrow-schema = "59.1.0"
parquet = { version = "59.1.0", features = ["arrow"] }
